 
numerator = float(input("What number are we dividing with? "))
denominator = float(input("What number are we dividng by? "))

def calculate_remainder(numerator, denominator):
    return numerator%denominator

print(calculate_remainder(numerator, denominator))